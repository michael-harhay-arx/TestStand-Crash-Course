/***************************************************************************//*!
* \page tips Tips & Tricks
*
* The following tips and tricks are helpful to know.
*
* - **Help Menu**: TestStand (weirdly) has two help menus, which can be accessed
* via **F1** and **Ctrl+F1**. The former provides context-sensitive help, while
* the latter contains all help documentation. It is recommended that you use 
* the full help menu (**Ctrl+F1**).
* 
* - **Running Selected Test Steps**: A set of specific test steps can be independently
* run, by selecting them in the Step Pane and right clicking, then selecting **Run Selected
* Steps** from the dropdown.
*
* - **Unload All Modules**: All currently held modules (typically .dlls) can be
* released by selecting **File > Unload All Modules**.
*
* - **Reset UI Configuration**: TestStand's UI can be reset to its default state
* by selecting **View > Reset UI Configuration**.
*
* - **Lock UI Configuration**: TestStand's UI can be locked to prevent further
* changes. To toggle this setting, select **View > Lock/Unlock UI Configuration**.
*
* - **Drag & Drop Variables**: TestStand variables can easily be dragged and dropped
* into most menus and text boxes. For example, instead of typing "Locals.ExampleLongVariableName"
* into a statement test step's expression editor, you can simply drag the variable
* from the Variables Pane.
*
* - **Expression Browser**: The expression browser contains a variety of
* tools that can be helpful if you get stuck while writing an expression.
* The red checkmark button is also useful, as it checks the current expression
* for errors.
*
*******************************************************************************/